<h1><%= @artist.name %></h1>
<p>Artist show page should list each song for the artist, and use the route resource route helper to link each song to its corresbonding show page.</p>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %></li>
  <% end %>
</ul>
